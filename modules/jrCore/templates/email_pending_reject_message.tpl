Your item has been rejected for the following:

Reason(s):
{$reject_reason}

{if strlen($reject_message) > 0}
Message:
{$reject_message}
{/if}

Once you fix these issues it can be viewable on your profile.

